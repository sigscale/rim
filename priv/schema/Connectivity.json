{
	"$id": "http://sigscale.org/schema/Connectivity",
	"title": "Connectivity",
	"description": "An edge in an adjacency graph of consituent resource functions",
	"type": "object",
	"properties": {
		"name": {
			"description": "Descriptive name for association",
			"type": "string"
		},
		"type": {
			"description": "Association type",
			"type": "string"
		},
		"endpoint": {
			"type": "array",
			"items": {
				"description": "Endpoints in an adjacency association",
				"type": "object",
				"properties": {
					"href": {
						"description": "Reference of an associated resource function",
						"type": "string"
					},
					"id": {
						"description": "Unique identifier of an associated resource function",
						"type": "string"
					},
					"name": {
						"description": "Name of the associated resource function",
						"type": "string"
					},
					"isRoot": {
						"description": "Directionality: true when endpoint is a source, false when a sink. If true for all endpoints connectivity is bidirectional. Default is true.",
						"type": "boolean"
					},
					"connectionPoint": {
						"description": "The connection point of the associated resource function",
						"$ref": "ConnectionPointRef"
					}
				},
				"required": [
					"href"
				]
			},
			"minItems": 2
		}
	},
	"required": [
		"type",
		"endpoint"
	]
}
